# 🚀 Claude Skills 配置指南

> 小红书 & TikTok Claude Skills 完整配置说明

---

## 📋 前置要求

在开始配置之前，请确保你的电脑已安装以下软件：

### 必需软件

1. **Node.js** (版本 18.0 或更高)
   - 下载地址：https://nodejs.org/
   - 安装后验证：打开命令行/终端，运行 `node --version`
   - 应显示类似：`v20.x.x` 或 `v18.x.x`

2. **Claude Code** 或 **Cursor** (带 Claude Code 插件)
   - Claude Code：https://claude.ai/code
   - Cursor：https://cursor.sh/
   - 任意一个即可

3. **Git** (可选，用于克隆项目)
   - 下载地址：https://git-scm.com/
   - 如果直接下载 ZIP 文件，则不需要安装

---

## 📦 安装步骤

### 方式 1：使用自动配置脚本（推荐，仅 Windows）

#### 步骤 1：获取项目文件

**选项 A：下载 ZIP**
1. 下载项目 ZIP 文件到本地
2. 解压到任意目录，例如：`C:\claude-skills`

**选项 B：使用 Git**
```bash
git clone <项目地址> claude-skills
cd claude-skills
```

#### 步骤 2：运行自动配置脚本

双击运行：
```
quick-setup.bat
```

脚本会自动：
- ✅ 检查 Node.js 是否已安装
- ✅ 安装所有依赖
- ✅ 显示配置代码和路径

#### 步骤 3：配置 Claude Code

打开脚本输出的配置文件位置（见下方"详细配置步骤"）

---

### 方式 2：手动配置（Windows/Mac/Linux）

#### 步骤 1：获取项目文件

同上（下载 ZIP 或使用 Git）

#### 步骤 2：安装依赖

**Windows (CMD):**
```cmd
cd claude-skills
cd xiaohongshu-skill
npm install
cd ..\tiktok-skill
npm install
cd ..
```

**Windows (PowerShell):**
```powershell
cd claude-skills
cd xiaohongshu-skill
npm install
cd ..\tiktok-skill
npm install
cd ..
```

**Mac/Linux:**
```bash
cd claude-skills
cd xiaohongshu-skill
npm install
cd ../tiktok-skill
npm install
cd ..
```

**如果安装很慢，使用国内镜像：**
```bash
npm install --registry=https://registry.npmmirror.com
```

---

## ⚙️ Claude Code 配置

### 找到配置文件

#### Windows 配置文件位置

**路径 1 (推荐):**
```
C:\Users\你的用户名\AppData\Roaming\Cursor\User\globalStorage\mcp.json
```

**路径 2:**
```
C:\Users\你的用户名\AppData\Roaming\Lingma\SharedClientCache\extension\local\mcp.json
```

**如何找到：**
1. 按 `Win + R` 打开运行对话框
2. 输入：`%APPDATA%\Cursor\User\globalStorage`
3. 按回车
4. 查找 `mcp.json` 文件

**如果没有 `mcp.json` 文件：**
- 创建一个新的文本文件
- 重命名为 `mcp.json`
- 确保文件扩展名是 `.json`（不是 `.txt`）

#### Mac 配置文件位置

```
~/Library/Application Support/Cursor/User/globalStorage/mcp.json
```

**如何找到：**
1. 打开 Finder
2. 按 `Cmd + Shift + G`
3. 输入：`~/Library/Application Support/Cursor/User/globalStorage`
4. 按回车
5. 查找或创建 `mcp.json` 文件

#### Linux 配置文件位置

```
~/.config/Cursor/User/globalStorage/mcp.json
```

---

### 编辑配置文件

**⚠️ 重要提示：**
- 记得修改路径为你的实际项目路径！
- Windows 路径使用双反斜杠 `\\` 或正斜杠 `/`
- Mac/Linux 路径使用正斜杠 `/`

#### Windows 配置示例

假设你的项目在：`C:\Users\YourName\claude-skills`

```json
{
  "skillsPath": "C:\\Users\\YourName\\claude-skills"
}
```

或者使用正斜杠：
```json
{
  "skillsPath": "C:/Users/YourName/claude-skills"
}
```

#### Mac/Linux 配置示例

假设你的项目在：`/Users/yourname/claude-skills`

```json
{
  "skillsPath": "/Users/yourname/claude-skills"
}
```

#### 完整配置文件格式

```json
{
  "skillsPath": "你的完整路径/claude-skills"
}
```

**注意事项：**
- 路径必须是**绝对路径**（完整路径）
- 不要使用相对路径（如 `./claude-skills`）
- 路径中不要包含特殊字符
- 确保路径正确指向包含 `xiaohongshu-skill` 和 `tiktok-skill` 文件夹的目录

---

### 保存配置文件

1. 将上面的 JSON 内容复制到 `mcp.json` 文件中
2. 修改 `skillsPath` 为你的实际路径
3. 保存文件
4. 确保文件编码是 UTF-8

---

## 🔄 重启 Claude Code

配置完成后，必须重启才能加载 Skills：

1. **完全关闭** Claude Code 或 Cursor
   - 不是最小化
   - 不是关闭窗口
   - 是完全退出应用

2. **重新打开** Claude Code 或 Cursor

3. Skills 会自动加载

---

## ✅ 验证配置

### 方法 1：检查 Skills 是否被识别

在 Claude Code 中，输入以下测试：

```
列出所有可用的 Skills
```

或者

```
你可以做什么？
```

**检查是否包含：**
- xiaohongshu-scraper (小红书爬虫)
- tiktok-video-scraper (TikTok爬虫)

### 方法 2：实际测试

**测试小红书 Skill：**
```
请帮我爬取这个小红书帖子：https://www.xiaohongshu.com/discovery/item/6476b9c20000000011011309
```

**测试 TikTok Skill：**
```
请帮我爬取这个TikTok视频：https://www.tiktok.com/@goodluck0311/video/7523066743128968455
```

---

## 🔧 首次使用

### 小红书 Skill 首次使用

1. **首次登录（一次性操作）**
   ```bash
   cd claude-skills/xiaohongshu-skill
   node scripts/login.js
   ```

   会打开浏览器窗口，请在浏览器中：
   - 扫码登录 或 密码登录
   - 登录成功后，按 `Ctrl+C` 退出
   - Cookies 会自动保存

2. **开始使用**
   - 在 Claude Code 中直接对话
   - 或使用命令行：
     ```bash
     node scripts/scrape.js "帖子URL"
     ```

### TikTok Skill 首次使用

1. **首次登录（一次性操作）**
   ```bash
   cd claude-skills/tiktok-skill
   node scripts/login.js
   ```

   会打开浏览器窗口，请在浏览器中：
   - 完成登录
   - 登录成功后，按 `Ctrl+C` 退出
   - Cookies 会自动保存

2. **开始使用**
   - 在 Claude Code 中直接对话
   - 或使用命令行：
     ```bash
     node scripts/scrape.js "视频URL"
     ```

---

## 📁 项目文件说明

配置完成后，你的目录结构应该是：

```
claude-skills/
│
├── xiaohongshu-skill/           # 小红书 Skill
│   ├── SKILL.md                 # Skill 定义（核心）
│   ├── package.json             # 依赖配置
│   ├── xhs_cookies.json         # 登录状态（自动生成）
│   └── scripts/                 # 可执行脚本
│       ├── scrape.js            # 爬取帖子
│       ├── get-stats.js         # 获取统计
│       └── login.js             # 登录辅助
│
└── tiktok-skill/                # TikTok Skill
    ├── SKILL.md                 # Skill 定义（核心）
    ├── package.json             # 依赖配置
    ├── tiktok_cookies.json      # 登录状态（自动生成）
    └── scripts/                 # 可执行脚本
        ├── scrape.js            # 爬取视频
        ├── get-stats.js         # 获取统计
        └── login.js             # 登录辅助
```

---

## 🐛 常见问题

### Q1: 找不到 mcp.json 文件

**解决方案：**

1. 确认已运行过至少一次 Claude Code/Cursor
2. 检查路径是否正确
3. 如果找不到，手动创建：
   - 在对应目录创建新文件
   - 命名为 `mcp.json`
   - 粘贴配置内容

### Q2: Claude Code 识别不到 Skills

**解决方案：**

1. **检查路径配置**
   - 确认使用的是绝对路径
   - 路径分隔符正确（Windows 用 `\\` 或 `/`）

2. **检查文件结构**
   - 确认 `xiaohongshu-skill/SKILL.md` 存在
   - 确认 `tiktok-skill/SKILL.md` 存在

3. **完全重启**
   - 关闭所有 Claude Code/Cursor 窗口
   - 重新打开

4. **检查依赖**
   ```bash
   cd claude-skills
   cd xiaohongshu-skill && npm install
   cd ../tiktok-skill && npm install
   ```

### Q3: 提示"模块未找到"或"command not found"

**解决方案：**

1. 确认 Node.js 已安装：
   ```bash
   node --version
   ```

2. 重新安装依赖：
   ```bash
   cd claude-skills
   cd xiaohongshu-skill
   npm install
   cd ../tiktok-skill
   npm install
   ```

### Q4: 脚本运行报错

**解决方案：**

1. **检查 Playwright**
   ```bash
   cd xiaohongshu-skill
   npx playwright install chromium
   ```

2. **检查登录状态**
   - 删除 `xhs_cookies.json` 或 `tiktok_cookies.json`
   - 重新运行 `node scripts/login.js`

### Q5: 爬取失败或数据不全

**可能原因：**
1. 登录状态过期 → 重新登录
2. 帖子/视频被删除或设为私密 → 尝试其他链接
3. 网络问题 → 检查网络连接
4. 页面加载未完成 → 等待更长时间

---

## 📊 功能说明

### 小红书 Skill 功能

| 功能 | 说明 |
|-----|------|
| **登录** | 手动登录一次，cookies 自动保存 |
| **爬取内容** | 标题、作者、正文、图片 |
| **获取统计** | 点赞、收藏、评论、分享 |
| **预览量** | ❌ 平台限制，无法获取 |

### TikTok Skill 功能

| 功能 | 说明 |
|-----|------|
| **登录** | 手动登录一次，cookies 自动保存 |
| **爬取内容** | 标题、作者、描述、标签 |
| **获取统计** | 点赞、评论、分享、收藏 |
| **播放量** | ❌ 平台限制，仅作者可见 |

---

## 📖 使用示例

### 在 Claude Code 中使用

配置完成后，直接对话即可：

```
你：帮我爬取这个小红书帖子 https://www.xiaohongshu.com/discovery/item/6476b9c20000000011011309

Claude：[自动调用 xiaohongshu-scraper Skill]

✅ 爬取成功！

帖子信息：
标题：为什么要有可恶的考试周
作者：小米好吃
点赞：2
...

你：获取这个TikTok视频的统计数据 https://www.tiktok.com/@user/video/...

Claude：[自动调用 tiktok-video-scraper Skill]

✅ 获取成功！

统计数据：
点赞：1,325
评论：11
分享：430
收藏：1,064
...
```

### 命令行使用

**小红书：**
```bash
cd claude-skills/xiaohongshu-skill

# 爬取完整内容
node scripts/scrape.js "帖子URL"

# 只获取统计数据
node scripts/get-stats.js "帖子URL"
```

**TikTok：**
```bash
cd claude-skills/tiktok-skill

# 爬取完整内容
node scripts/scrape.js "视频URL"

# 只获取统计数据
node scripts/get-stats.js "视频URL"
```

---

## 🎯 配置检查清单

使用此清单逐项检查配置是否正确：

### 安装前检查

- [ ] Node.js 已安装（v18+）
- [ ] Claude Code 或 Cursor 已安装
- [ ] 已获取项目文件

### 依赖安装

- [ ] 进入项目目录：`cd claude-skills`
- [ ] 安装小红书依赖：`cd xiaohongshu-skill && npm install && cd ..`
- [ ] 安装 TikTok 依赖：`cd tiktok-skill && npm install && cd ..`

### 配置文件

- [ ] 找到 mcp.json 文件
- [ ] 添加 skillsPath 配置
- [ ] 修改为实际的绝对路径
- [ ] 路径分隔符正确（Windows 用 `\\` 或 `/`，Mac/Linux 用 `/`）
- [ ] 保存文件

### 重启验证

- [ ] 完全关闭 Claude Code/Cursor
- [ ] 重新打开应用
- [ ] 测试列出可用 Skills
- [ ] 实际测试爬取功能

### 首次使用

- [ ] 小红书首次登录：`cd xiaohongshu-skill && node scripts/login.js`
- [ ] TikTok 首次登录：`cd tiktok-skill && node scripts/login.js`

---

## 📞 获取帮助

如果遇到问题：

1. 查看"常见问题"部分
2. 检查配置文件格式
3. 确认所有依赖已安装
4. 完全重启 Claude Code
5. 查看 SKILL.md 文件中的详细说明

---

## 🎉 配置完成

配置完成后，你就可以：

- ✅ 在 Claude Code 中使用自然语言调用 Skills
- ✅ 爬取小红书帖子内容和数据
- ✅ 爬取 TikTok 视频内容和数据
- ✅ 获取完整的统计数据

**享受使用！** 🚀

---

**最后更新：** 2024年12月31日
**版本：** 1.0.0
